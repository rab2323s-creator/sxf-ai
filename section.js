const body=document.body,category=body.dataset.category||'',feed=document.getElementById('sectionFeed'),empty=document.getElementById('sectionEmpty'),search=document.getElementById('sectionSearch'),lenses=[...document.querySelectorAll('.lens')];let items=[],activeKeywords='';document.getElementById('year').textContent=new Date().getFullYear();function esc(v=''){return String(v).replace(/[&<>'\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#039;','\"':'&quot;'}[c]))}function ago(v){const d=new Date(v),m=Math.max(0,Math.floor((Date.now()-d.getTime())/60000));if(m<2)return'just now';if(m<60)return m+'m ago';const h=Math.floor(m/60);if(h<24)return h+'h ago';const days=Math.floor(h/24);return days<7?days+'d ago':d.toLocaleDateString(undefined,{month:'short',day:'numeric'})}function match(x){if(!activeKeywords)return true;const words=activeKeywords.toLowerCase().split(/\s+/).filter(Boolean),hay=(x.title+' '+x.source).toLowerCase();return words.some(w=>hay.includes(w))}function card(x){return '<a class=\"intel-card\" href=\"'+esc(x.signal_url||x.url)+'\"><div class=\"intel-meta\"><strong>'+esc(x.source)+'</strong><span>'+esc(ago(x.published))+'</span></div><h3>'+esc(x.title)+'</h3><div class=\"intel-foot\"><span>'+esc(x.category)+'</span><b>↗</b></div></a>'}function render(){const q=(search?.value||'').trim().toLowerCase(),visible=items.filter(x=>match(x)&&(!q||(x.title+' '+x.source).toLowerCase().includes(q)));feed.innerHTML=visible.map(card).join('');empty.hidden=visible.length!==0;document.getElementById('sectionCount').textContent=items.length}lenses.forEach(b=>b.addEventListener('click',()=>{lenses.forEach(x=>x.classList.remove('active'));b.classList.add('active');activeKeywords=b.dataset.keywords||'';render()}));search?.addEventListener('input',render);fetch('../data/news.json',{cache:'no-cache'}).then(r=>r.json()).then(d=>{items=(d.items||[]).filter(x=>x.category===category);render()}).catch(()=>{});