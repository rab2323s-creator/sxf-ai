const state={items:[],filter:'All',query:''};const feed=document.getElementById('feed'),empty=document.getElementById('emptyState'),search=document.getElementById('searchInput'),filters=[...document.querySelectorAll('.filter')];document.getElementById('year').textContent=new Date().getFullYear();function escapeHtml(value=''){return value.replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#039;','"':'&quot;'}[c]))}function relativeTime(dateStr){const d=new Date(dateStr);if(Number.isNaN(d.getTime()))return'';const diff=Date.now()-d.getTime(),h=Math.floor(diff/3600000);if(h<1)return'Just now';if(h<24)return h+'h ago';const days=Math.floor(h/24);if(days<7)return days+'d ago';return d.toLocaleDateString(undefined,{month:'short',day:'numeric'})}function render(){const q=state.query.trim().toLowerCase();const visible=state.items.filter(item=>{const categoryMatch=state.filter==='All'||item.category===state.filter;const queryMatch=!q||(`${item.title} ${item.source} ${item.category}`).toLowerCase().includes(q);return categoryMatch&&queryMatch});feed.innerHTML=visible.map(item=>`<a class="story" href="${escapeHtml(item.url)}" target="_blank" rel="noopener noreferrer"><div class="story-meta"><span class="story-source">${escapeHtml(item.source)}</span><span>${escapeHtml(relativeTime(item.published))}</span></div><div><h2 class="story-title">${escapeHtml(item.title)}</h2><span class="story-category">${escapeHtml(item.category)}</span></div><span class="story-arrow" aria-hidden="true">↗</span></a>`).join('');empty.hidden=visible.length!==0;document.getElementById('storyCount').textContent=state.items.length+' updates'}filters.forEach(btn=>btn.addEventListener('click',()=>{filters.forEach(b=>b.classList.remove('active'));btn.classList.add('active');state.filter=btn.dataset.filter;render()}));search.addEventListener('input',e=>{state.query=e.target.value;render()});fetch('./data/news.json',{cache:'no-store'}).then(r=>{if(!r.ok)throw new Error('Could not load news');return r.json()}).then(data=>{state.items=Array.isArray(data.items)?data.items:[];document.getElementById('lastUpdated').textContent=data.updated_at?'Updated '+relativeTime(data.updated_at):'Updated automatically';render()}).catch(()=>{document.getElementById('lastUpdated').textContent='Feed temporarily unavailable';empty.hidden=false;empty.textContent='The automated feed has not been generated yet.'});